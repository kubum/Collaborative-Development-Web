<ul id="nav">
	<%= li_featured "Featured products", stocks_sort_path(:featured => :featured), params %>
	<%= li_all_products "All products",  stocks_sort_path, params %>
	
	<% StockCategory.all.each do |category| %>
	    <%= li_left_category category.title, { :category_id => category.id }, params %>
	<% end %>
</ul>